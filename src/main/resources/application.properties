# Database Configuration - Use environment variables for cloud deployment
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/crm?useSSL=false}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:password}

# Connection Pool Configuration for cloud environments
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:5}
spring.datasource.hikari.connection-timeout=${DB_CONN_TIMEOUT:30000}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME:1800000}

# Management and Actuator Configuration
management.security.enabled=false
management.context-path=/appinfo
management.endpoints.web.exposure.include=health,info,metrics

# Thymeleaf Configuration
spring.thymeleaf.mode=LEGACYHTML5
spring.thymeleaf.cache=${THYMELEAF_CACHE:false}

# Google Cloud Storage Configuration
gcs.bucket.name=${GCS_BUCKET_NAME:default-bucket}
gcs.pdf.folder=${GCS_PDF_FOLDER:pdfs}
gcs.csv.folder=${GCS_CSV_FOLDER:csv}

# Server Configuration - Use environment variable for port
server.port=${PORT:8080}

# Logging Configuration for cloud environments (JSON format for better log aggregation)
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.level.root=${LOG_LEVEL:INFO}
logging.level.crm=${APP_LOG_LEVEL:DEBUG}

# Cloud-specific configurations
spring.cloud.gcp.project-id=${GCP_PROJECT_ID:}
spring.cloud.gcp.credentials.location=${GCP_CREDENTIALS_LOCATION:}
