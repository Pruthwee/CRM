# Database Configuration - Externalized for Cloud
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/crm?useSSL=false}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:password}

# Connection Pool Configuration for Cloud
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN_IDLE:2}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME:1800000}

# Management/Actuator Configuration
management.security.enabled=false
management.context-path=/appinfo

# Thymeleaf Configuration
spring.thymeleaf.mode=LEGACYHTML5
spring.thymeleaf.cache=${THYMELEAF_CACHE:false}

# Cloud Storage Configuration for GCP
gcp.storage.bucket-name=${GCP_STORAGE_BUCKET:crm-files-bucket}
gcp.storage.project-id=${GCP_PROJECT_ID:}
gcp.storage.credentials-path=${GCP_CREDENTIALS_PATH:}

# File Upload Configuration
file.upload.temp-dir=${TEMP_DIR:/tmp}

# Timezone Configuration - Use UTC for cloud consistency
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
